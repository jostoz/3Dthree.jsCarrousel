import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Josué Tostado - AI Engineer Portfolio",
  description: "Senior AI Engineer & Machine Learning Specialist with 10+ years building intelligent systems. Explore my portfolio of cutting-edge AI projects.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
